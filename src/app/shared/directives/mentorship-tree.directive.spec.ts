import { MentorshipTreeDirective } from './mentorship-tree.directive';

describe('MentorshipTreeDirective', () => {
  it('should create an instance', () => {
    const directive = new MentorshipTreeDirective();
    expect(directive).toBeTruthy();
  });
});
