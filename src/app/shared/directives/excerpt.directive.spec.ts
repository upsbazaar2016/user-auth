import { ExcerptDirective } from './excerpt.directive';

describe('ExcerptDirective', () => {
  it('should create an instance', () => {
    const directive = new ExcerptDirective();
    expect(directive).toBeTruthy();
  });
});
